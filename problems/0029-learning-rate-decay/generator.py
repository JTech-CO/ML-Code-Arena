"""학습률 감쇠 케이스.

세 스케줄을 모두 덮되, **계단형에 경계를 몰아준다**. `step_size = 1` 은 매 스텝
감쇠하므로 정수 나눗셈을 실수 나눗셈으로 잘못 쓴 풀이와 값이 같아진다 — 그래서
`step_size` 가 2 이상인 케이스가 판정을 담당한다.

`total_steps = 1` 은 코사인의 분모 규약을 시험한다. `total_steps - 1` 로 나눈 풀이는
여기서 0 으로 나눈다.

`gamma = 1.0` 은 감쇠가 없는 경계다. 세 스케줄 중 앞의 둘이 상수를 낸다.
"""

SEED = 20260829

CASES = [
    ("step", 1.0, 7, 0.5, 3, 0.0),
    ("cosine", 1.0, 3, 0.0, 1, 0.0),
    ("step", 0.1, 20, 0.9, 5, 0.0),
    ("step", 1.0, 10, 0.5, 1, 0.0),
    ("step", 2.0, 1, 0.5, 4, 0.0),
    ("step", 1.0, 12, 1.0, 3, 0.0),
    ("exponential", 1.0, 10, 0.1, 1, 0.0),
    ("exponential", 0.05, 100, 0.05, 2, 0.0),
    ("exponential", 1.0, 5, 0.0, 1, 0.0),
    ("cosine", 1.0, 1, 0.5, 2, 0.0),
    ("cosine", 0.1, 50, 0.5, 2, 0.001),
    ("cosine", 1.0, 1000, 0.5, 2, 0.01),
    ("step", 1.0, 1000, 0.99, 7, 0.0),
]


def generate():
    for case in CASES:
        yield case
