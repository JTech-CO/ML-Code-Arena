"""경사하강 케이스.

**발산하는 케이스**를 반드시 넣는다. 학습률이 크면 값이 튀어 오르며, 이것이 학습률을
왜 조심해야 하는지 보여 주는 유일한 방법이다. 발산을 감지해 멈추는 풀이는 궤적 길이가
달라 `length_mismatch` 로 잡힌다.

`b ≠ 0` 케이스를 넣는다. `b = 0` 만 주면 최솟값이 원점이라 부호를 틀려도 대칭 때문에
값이 우연히 맞는 구간이 생긴다.

`steps = 0` 은 `[x0]` 하나만 반환하는 경계다.

발산 케이스의 스텝 수는 작게 둔다. `1.5^200` 은 `inf` 가 되고 `inf` 는 비교에서
값이 아니라 특수값이 된다.
"""

SEED = 20260826

CASES = [
    # (a, b, c, x0, lr, steps)
    (1.0, 0.0, 0.0, 1.0, 0.1, 3),
    (1.0, 0.0, 0.0, 5.0, 0.01, 200),
    (1.0, -4.0, 7.0, 0.0, 0.1, 100),
    (0.5, 3.0, -2.0, 10.0, 0.05, 150),
    (2.0, 1.0, 0.0, -3.0, 0.2, 50),
    (1.0, 0.0, 0.0, 1.0, 0.5, 10),
    # 발산 — lr > 1/a
    (1.0, 0.0, 0.0, 1.0, 1.2, 20),
    (0.25, 2.0, 0.0, 4.0, 5.0, 15),
    # 경계
    (1.0, 0.0, 0.0, 3.0, 0.0, 5),
    (1.0, 2.0, 3.0, 1.0, 0.1, 0),
    # lr = 1/(2a) — 한 스텝에 최솟값으로 간다.
    (1.0, -6.0, 0.0, 100.0, 0.5, 4),
    (0.01, 0.0, 0.0, 1000.0, 1.0, 500),
]


def generate():
    for case in CASES:
        yield case
