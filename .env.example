# docs/ENVIRONMENT.md §3 — 값은 비워 둔다. 실제 값이 담긴 .env 는 커밋 금지 (INV-1).
# SESSION_SECRET 과 IP_HASH_SECRET 은 반드시 서로 다른 값이어야 한다.

DATABASE_URL=
REDIS_URL=
SESSION_SECRET=
IP_HASH_SECRET=
JUDGE_IMAGE=mlca-python:3.11
JUDGE_WORK_DIR=
WORKER_CONCURRENCY=4
NODE_ENV=
